<% clazz ||= '' %>
<%= form_for @user_taking_course, :url => path, html: {class: clazz + ' form-horizontal'} do |f| %>
  <%= render :partial => 'take_fields', :locals => {:f => f} %>
    <br>
    <div class="center">
      <%= f.submit(t('course.mark_taking'), :class=>'btn btn-success form-control') %>
    </div>
<% end %>