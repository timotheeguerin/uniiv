<% @courses.each do |course| %>
    <li>

      <div data-course-id='<%= course.id %>' data-type='new' data-need-reload=<%= params[:need_reload] %>>
        <h5><%= course.to_s %>
        </h5>
      </div>
    </li>
<% end %>